// pacote para a função prompt funcionar
const prompt = require('prompt-sync')();

//Descreva operação clear usando apenas operações serve e emplenty.
function clear(lista){
    while (lista =! empty()){
        lista.serve()
    }
}

