// pacote para a função prompt funcionar
const prompt = require('prompt-sync')();
 
//PILHA - algoritimos clear usando apenas emplty e pop e operação top usando apenas push, pop e emply.
 
/*
função clear()
  while(!Empty()){
      Pop()
  }
}
 
função top(){
  if(!Empty()){
    x = pop()
    push(x)
    return x
  }
}
*/
 